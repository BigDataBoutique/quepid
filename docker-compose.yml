version: '3'   # on CircleCI the version and services cause build to puke.
services:
  mysql:
    container_name: quepid_db
    image: mysql:5.6.37
    ports:
      - 3306:3306
    environment:
      - MYSQL_ROOT_PASSWORD=password

  redis:
    container_name: quepid_redis
    image: redis
    ports:
      - 6379:6379

  app:
    container_name: quepid_app
    #build:
    #  context: .
    #  dockerfile: Dockerfile.dev
    #image: quepid/app
    image: registry.gitlab.com/eyfo/quepid:commit-0a781fb1d632093b7320fccab9e4743d0b4b7f3b
    environment:
      - PORT=3000
      - RACK_ENV=development
      - RAILS_ENV=development
      - DB_ADAPTER=mysql2
      - DB_HOST=mysql
      - DB_USERNAME=root
      - DB_PASSWORD=password
      - DB_NAME=quepid
      - REDIS_URL=redis://redis:6379/1
    volumes:
      - .:/srv/app
    ports:
      - 3000:3000
    links:
      - mysql
      - redis
    depends_on:
      - mysql
      - redis
